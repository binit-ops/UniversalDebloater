<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp"
    android:background="@drawable/app_bg">

    <!-- Search Bar with Glass UI -->
    <EditText
        android:id="@+id/searchBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Search apps or packages..."
        android:textColorHint="#99FFFFFF"
        android:textColor="#FFFFFF"
        android:padding="16dp"
        android:background="@drawable/glass_bg"
        android:layout_marginBottom="12dp"
        android:layout_alignParentTop="true"/>

    <!-- List View (Dividers removed so cards float) -->
    <ListView
        android:id="@+id/listView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/searchBar"
        android:layout_above="@id/bottomBar"
        android:divider="@android:color/transparent"
        android:dividerHeight="8dp"/>

    <!-- Bottom Control Panel with Glass UI -->
    <LinearLayout
        android:id="@+id/bottomBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal"
        android:background="@drawable/glass_bg"
        android:padding="12dp"
        android:gravity="center"
        android:layout_marginTop="12dp">

        <Button
            android:id="@+id/btnLoadProfile"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Load"
            android:background="@drawable/btn_squircle"
            android:textColor="#FFFFFF"
            android:layout_marginEnd="8dp"/>

        <Button
            android:id="@+id/btnSaveProfile"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Save"
            android:background="@drawable/btn_squircle"
            android:textColor="#FFFFFF"
            android:layout_marginEnd="8dp"/>

        <Button
            android:id="@+id/btnNuke"
            android:layout_width="0dp"
            android:layout_weight="1.5"
            android:layout_height="wrap_content"
            android:text="NUKE"
            android:background="@drawable/btn_nuke_squircle"
            android:textColor="#FFFFFF"/>
    </LinearLayout>

</RelativeLayout>
            
